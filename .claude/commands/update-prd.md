---
description: Update the Coffee Brewing Calculator PRD in Notion (via the SPM) to reflect a feature or change
---

Update the project PRD in Notion to reflect the following feature or change:

$ARGUMENTS

Follow the project's Feature Discussion Protocol (see CLAUDE.md). Notion is the single source of truth — always connect via the Notion MCP and write the changes; never just summarize. Never assume; ask Ishaan when anything is unclear.

1. **Clarify first.** If the change above is unclear or under-specified, interview Ishaan with clarifying questions before proceeding. If the arguments are empty, instead perform a full cross-verification of the PRD against the currently shipped application and sync all Product Documentation.
2. **Loop the SPM** (Senior Product Manager agent at `D:\Pet Project\Agents`):
   - Share the feature/change context.
   - Have the SPM cross-verify and update the relevant PRD sections (functional requirements, release scope, open questions). PRD page id: `3836533a-dbe4-813c-b1fe-cb584648ebbf`.
   - Have it append a Decision Log entry (next sequential number, `3836533a-dbe4-818e-b380-c8eff3829730`) and update the Roadmap (`3836533a-dbe4-81cb-9c09-f29b2cc6bad6`) / Architecture (`3836533a-dbe4-81ae-9449-e8fd1ab53577`) if affected.
   - Bring back any clarifying questions the SPM raises.
3. **Confirm** those questions with Ishaan, then have the SPM finalize the Notion updates.
4. **Report** what changed: which pages were updated, the Decision Log number used, and any remaining open questions. Keep the PRD Status accurate and preserve history (annotate, don't overwrite).
