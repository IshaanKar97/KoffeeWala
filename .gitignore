node_modules
dist
dist-ssr
*.local
.env
.env.*
!.env.example
.DS_Store
*.log

# Local Netlify folder
.netlify

# Secrets — never commit (OAuth client secrets, service keys, etc.)
OAuth_Secret.json
*secret*.json
*credentials*.json

# Claude Code local tooling
skills-lock.json
